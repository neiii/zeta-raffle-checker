# zeta-raffle-checker
to run:
1. download everything
2. put all of your addresses in `eth.csv`
3. open terminal at folder
4. run `pip install -r requirements.txt`
5. run `python3 main.py`
6. `results.csv` will appear, all the results will be listed there
